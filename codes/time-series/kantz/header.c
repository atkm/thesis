#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#define NX 50000
#define MM 15
#define MAX(a,b) (a)>(b) ? (a) : (b)
#define MIN(a,b) (a)<(b) ? (a) : (b)
#define LOG(a) (((a)>1e-20)? log((a)): log(1e-20))
